---
- name: Homelab services deployment
  hosts: localhost
  become: true
  vars_files:
      - vault.yml
  roles:
      - bitwarden
      - dumbassets
      - dumbbudget
      - dumbdo
      - dumbkan
      - dumbpad
      - gitea
      - glance
      - jellyfin
      - memos
      - pocket-id
      - searxng
      # - tsidp
