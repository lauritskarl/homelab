services:
    ts-dumbdo:
        image: tailscale/tailscale:latest
        container_name: ts-dumbdo
        hostname: dumbdo
        environment:
            - TS_AUTHKEY=$TS_AUTHKEY
            - TS_STATE_DIR=/var/lib/tailscale
            - TS_USERSPACE=false
            - TS_SERVE_CONFIG=/config/serve-config.json
        volumes:
            - $PWD/data/ts-dumbdo/state:/var/lib/tailscale
            - $PWD/data/ts-dumbdo/config:/config
        devices:
            - /dev/net/tun:/dev/net/tun
        cap_add:
            - net_admin
        restart: unless-stopped
    dumbdo:
        image: dumbwareio/dumbdo:latest
        container_name: dumbdo
        network_mode: service:ts-dumbdo
        depends_on:
            - ts-dumbdo
        volumes:
            - "$PWD/data/dumbdo:/app/data"
        restart: unless-stopped
