services:
    ts-searxng:
        image: tailscale/tailscale:latest
        container_name: ts-searxng
        hostname: searxng
        environment:
            - TS_AUTHKEY=$TS_AUTHKEY
            - TS_STATE_DIR=/var/lib/tailscale
            - TS_USERSPACE=false
            - TS_SERVE_CONFIG=/config/serve-config.json
        volumes:
            - $PWD/data/ts-searxng/state:/var/lib/tailscale
            - $PWD/data/ts-searxng/config:/config
        devices:
            - /dev/net/tun:/dev/net/tun
        cap_add:
            - net_admin
        restart: unless-stopped
    searxng:
        image: docker.io/searxng/searxng:latest
        container_name: searxng
        network_mode: service:ts-searxng
        depends_on:
            - ts-searxng
        volumes:
            - "$PWD/data/searxng/config:/etc/searxng/"
            - "$PWD/data/searxng/data:/var/cache/searxng/"
        restart: unless-stopped
